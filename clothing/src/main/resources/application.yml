server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clothing_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

# 文件上传配置
upload:
  dir: e:/workbase/clothing/uploads
  base-url: http://localhost:8080/uploads
