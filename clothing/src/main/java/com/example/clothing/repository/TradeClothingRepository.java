package com.example.clothing.repository;

import com.example.clothing.entity.TradeClothing;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TradeClothingRepository extends JpaRepository<TradeClothing, Long> {
}
